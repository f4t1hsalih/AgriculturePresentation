@using EntityLayer.Concrete
@model List<Service>
@{
    //Buradaki Title özelliği sekme başlığına yazılacak özelliktir
    ViewData["Title"] = "Hizmetler";
    //Buradaki Layout ise sayfada kullanılan layout'un konumunu bildirir
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Hizmetler</h4>
            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Başlık</th>
                            <th>Resim Yolu</th>
                            <th>Açıklama</th>
                            <th>Düzenle</th>
                            <th>Sil</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.ServiceID</td>
                                <td>@item.Title</td>
                                <td>@item.Image</td>
                                <td>@item.Description</td>
                                <td><a href="/Service/UpdateService/@item.ServiceID" class="btn btn-outline-twitter">Düzenle</a></td>
                                <td><a href="/Service/DeleteService/@item.ServiceID" class="btn btn-outline-danger">Sil</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <br />
                <a href="/Service/AddService/" class="btn btn-outline-info">Yeni Hizmet Girişi</a>
            </div>
        </div>
    </div>
</div>